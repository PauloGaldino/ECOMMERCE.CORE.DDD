@model Domain.Entities.Contatos.Email

@{
    ViewData["Title"] = "Deletar Email";
}

<h2>Deletar Email</h2>

<h3>Você tem certeza que deseja realmente deletar este registro?</h3>
<div>
    <h4>Email</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EnderecoEmail)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EnderecoEmail)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="EmailId" />
        <input type="submit" value="Deletar" class="btn btn-danger" /> |
        <a class="btn btn-primary" asp-action="Index">Voltar para a Lista</a>
    </form>
</div>
